# Supabase Database Configuration
# These variables are REQUIRED for the backend services to start.
# If they are missing, the services will immediately crash to ensure no fallback to localhost occurs.
SUPABASE_URL=jdbc:postgresql://<your-supabase-db-host>:5432/postgres
SUPABASE_USER=postgres.xxxxxxxxxxxxx
SUPABASE_PASSWORD=your_secure_password
